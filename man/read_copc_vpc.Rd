% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vpc.R
\name{read_copc_vpc}
\alias{read_copc_vpc}
\title{Read a Virtual Point Cloud (VPC) file}
\usage{
read_copc_vpc(
  path,
  chunk_size = 0,
  chunk_buffer = 0,
  select = "*",
  filter = "",
  trust_vpc = TRUE,
  progress = TRUE
)
}
\arguments{
\item{path}{Character. Path or URL to a \code{.vpc} file.}

\item{chunk_size}{Numeric. Default chunk size for catalog processing
(0 = file-level). Passed to \code{\link[=read_copc_catalog]{read_copc_catalog()}}.}

\item{chunk_buffer}{Numeric. Buffer around each chunk.}

\item{select}{Character. Default column selection.}

\item{filter}{Character. Default attribute filter string.}

\item{trust_vpc}{Logical. If \code{TRUE} (the default), build the catalog
directly from VPC metadata (bbox, CRS, point count) without
re-reading each COPC file header.  This is dramatically faster for
large collections (thousands of files).  Set to \code{FALSE} to force a
full header read for every tile (original behaviour).}

\item{progress}{Logical. Print progress messages?}
}
\value{
A \code{copc_catalog} or \code{lidR::LAScatalog} object. The VPC
metadata (statistics, overview paths, etc.) is stored in an
attribute \code{"vpc_metadata"}.
}
\description{
Reads a \code{.vpc} file (a STAC ItemCollection / GeoJSON FeatureCollection)
and returns a \code{copc_catalog} or \code{lidR::LAScatalog} ready for processing
with \code{\link[=copc_catalog_apply]{copc_catalog_apply()}} or \code{\link[=copc_apply]{copc_apply()}}.
}
\details{
A VPC file is a lightweight JSON index referencing multiple point cloud
files (typically \code{.copc.laz}) with their spatial extents, CRS, and
optional statistics.
This is the format used by QGIS and PDAL wrench for virtual point
clouds.
}
\note{
Currently only \code{.copc.laz} files are supported as referenced
tiles.Non-COPC formats (plain \code{.las}/\code{.laz}) are detected and
skipped with a warning.A fallback to \code{lidR::readLAS()} for full
QGIS parity is planned for a future release.
}
\examples{
\dontrun{
ctg <- read_copc_vpc("tiles.vpc")
print(ctg)

# Use with copc_apply()
results <- copc_apply(ctg, function(las) mean(las$Z),
                      chunk_size = 500)

# Fast path (default) -- trusts VPC metadata, no header re-reads
ctg_fast <- read_copc_vpc("big_project.vpc", trust_vpc = TRUE)

# Slow path -- re-reads every header for maximum accuracy
ctg_strict <- read_copc_vpc("big_project.vpc", trust_vpc = FALSE)
}

}
\seealso{
\code{\link[=write_copc_vpc]{write_copc_vpc()}}, \code{\link[=read_copc_catalog]{read_copc_catalog()}}, \code{\link[=copc_apply]{copc_apply()}}
}
