% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vpc.R
\name{write_copc_vpc}
\alias{write_copc_vpc}
\title{Write a Virtual Point Cloud (VPC) file}
\usage{
write_copc_vpc(
  paths,
  output,
  boundary = FALSE,
  statistics = FALSE,
  overview = NULL,
  overview_levels = 1L,
  progress = TRUE
)
}
\arguments{
\item{paths}{Character vector. Paths or URLs to \code{.copc.laz} files.}

\item{output}{Character. Output \code{.vpc} file path.}

\item{boundary}{Logical. If \code{TRUE}, compute the exact boundary polygon
for each file (from the convex hull of points at LOD 0). Default
\code{FALSE} uses the rectangular bounding box.}

\item{statistics}{Logical. If \code{TRUE}, compute per-attribute statistics
(min, max, mean, count) for each file.
Requires reading point data (slower). Default \code{FALSE}.}

\item{overview}{Character, logical, or \code{NULL}. A path to an existing
overview \code{.copc.laz}, \code{TRUE} to auto-generate a single overview
(1 in 1000 points), or \code{NULL} (default) for no overview.}

\item{overview_levels}{Integer. Number of hierarchical overview levels
to generate when \code{overview = TRUE}. Level 0 covers the full extent;
each subsequent level tiles into a 2x2 grid at higher density.
Default \code{1L} (single merged overview). Values > 1 produce a pyramid.}

\item{progress}{Logical. Print progress messages?}
}
\value{
Invisibly returns the output file path.
}
\description{
Creates a \code{.vpc} file (STAC ItemCollection / GeoJSON FeatureCollection)
from one or more COPC files.
The resulting VPC can be opened directly in QGIS or processed with
PDAL wrench.
}
\examples{
\dontrun{
# Build a VPC from local COPC tiles
files <- list.files("tiles/", pattern = "\\\\.copc\\\\.laz$",
                    full.names = TRUE)
write_copc_vpc(files, "my_project.vpc")

# With statistics and boundary polygons
write_copc_vpc(files, "my_project.vpc",
          boundary = TRUE, statistics = TRUE)
}

}
\seealso{
\code{\link[=read_copc_vpc]{read_copc_vpc()}}, \code{\link[=read_copc_catalog]{read_copc_catalog()}}
}
