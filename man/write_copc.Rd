% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_copc.R
\name{write_copc}
\alias{write_copc}
\title{Write a COPC LAZ File}
\usage{
write_copc(x, filename, max_depth = -1L, filter = NULL, progress = TRUE)
}
\arguments{
\item{x}{Point-cloud data in one of four forms (see \emph{Input types}).}

\item{filename}{Character.
Output file path (should end in \code{.copc.laz}).}

\item{max_depth}{Integer.
Maximum octree depth.
\code{-1} (default) = auto (targets ~10 000 pts/leaf, max 8).
\code{0} = all points in a single root node.}

\item{filter}{Character.
Optional LAStools-style attribute filter applied \strong{before} writing,
e.g. \code{"-keep_class 2 6"}.  Only matching points are written.}

\item{progress}{Logical.  Print progress messages?}
}
\value{
Invisibly returns \code{filename}.
}
\description{
Writes point-cloud data to a Cloud Optimized Point Cloud (COPC) LAZ
file.
}
\details{
The output is a valid \code{.copc.laz} file readable by any COPC-aware
software (PDAL, QGIS, CloudCompare, copc4R, \dots) \strong{and} any
standard LAZ reader (the point data is stored as a normal LAZ stream
with a chunk table).
}
\section{Input types}{

\code{write_copc()} is designed to "just work" with whatever you have:

\describe{
\item{\code{read_copc()} result}{A list with \verb{$data} + \verb{$header}.
Header metadata (CRS, scale, offset, point format) is preserved.}
\item{\code{lidR::LAS} object}{CRS, scale factors, offsets, and point
format are extracted from the LAS header automatically.}
\item{\code{data.table} / \code{data.frame}}{Must contain at least \code{X}, \code{Y},
\code{Z}.  Smart defaults are used: scale = 0.001, offsets computed
from data extent, point format inferred from columns present.}
\item{\code{sf} object}{Point or polygon geometry with LiDAR attributes
-- coordinates extracted, CRS preserved.}
}
}

\section{Point format}{

COPC requires LAS 1.4, point data record format 6, 7, or 8.
When no header is provided, the format is inferred from columns:

\describe{
\item{Format 6}{XYZ + intensity, classification, return info, \dots}
\item{Format 7}{Format 6 + R, G, B}
\item{Format 8}{Format 7 + NIR}
}
}

\section{Smart defaults (bare data.frame / data.table)}{

When writing a bare table without header metadata, \code{write_copc()}
mirrors the conventions used by lidR and rlas:

\itemize{
\item \strong{Scale factors}: 0.001 (millimetre precision)
\item \strong{Offsets}: \code{floor(min(X))}, \code{floor(min(Y))}, 0
\item \strong{Bbox / point count}: computed from data
\item \strong{Point format}: inferred from column names
\item \strong{CRS}: none (unless an \code{sf} object with CRS is passed)
}
}

\section{Column name normalisation}{

Common variants are silently mapped to rlas conventions:
\code{x}->\code{X}, \code{y}->\code{Y}, \code{z}->\code{Z}, \code{gps_time}->\code{gpstime},
\code{intensity}->\code{Intensity}, \code{classification}->\code{Classification},
\code{return_number}->\code{ReturnNumber}, etc.
}

\examples{
f <- system.file("extdata", "NoAZCampus_SWFSC_Bldg82_2mVoxel.copc.laz",
                 package = "copc4R")

# Round-trip from read_copc()
result <- read_copc(f, progress = FALSE)
tmp <- tempfile(fileext = ".copc.laz")
write_copc(result, tmp, progress = FALSE)
back <- read_copc(tmp, progress = FALSE)
nrow(back$data)
unlink(tmp)

\donttest{
# Write a lidR LAS object
if (requireNamespace("lidR", quietly = TRUE)) {
  las <- as_las(result)
  write_copc(las, tempfile(fileext = ".copc.laz"), progress = FALSE)
}

# Write a bare data.frame -- smart defaults kick in
df <- data.frame(X = runif(100), Y = runif(100), Z = runif(100))
write_copc(df, tempfile(fileext = ".copc.laz"), progress = FALSE)
}

}
