% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_aoi.R
\name{get_aoi}
\alias{get_aoi}
\title{Interactively draw an Area of Interest on a leaflet map}
\usage{
get_aoi(fallback_center = c(-111.6513, 35.1983), fallback_zoom = 12)
}
\arguments{
\item{fallback_center}{Numeric length-2 vector \code{c(lng, lat)} used when
geolocation is unavailable.}

\item{fallback_zoom}{Integer. Initial zoom level for the fallback view.}
}
\value{
An \code{sf} data frame containing a single polygon feature, or
\code{NULL} if no AOI was drawn.
}
\description{
Opens an interactive leaflet map with satellite imagery and lets the
user draw a rectangle AOI.
The map attempts browser geolocation on load; if denied it falls back
to the supplied centre and zoom.
}
\details{
Requires the \pkg{sf}, \pkg{leaflet}, \pkg{mapedit}, and
\pkg{htmlwidgets} packages.
Only the first drawn feature is retained.
}
\examples{
\dontrun{
aoi <- get_aoi()
tiles <- download_3dep_copc(aoi, dest_dir = "tiles/")
}

}
