% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiling.R
\name{read_copc_tiles}
\alias{read_copc_tiles}
\title{Read COPC data as a grid of tiles}
\usage{
read_copc_tiles(
  path_or_url,
  extent = NULL,
  tile_size,
  overlap = 0,
  select = NULL,
  filter = NULL,
  max_depth = -1L,
  resolution = NULL,
  max_points = Inf,
  progress = TRUE,
  as_las = FALSE
)
}
\arguments{
\item{path_or_url}{Character. Path or URL to a \code{.copc.laz} file.}

\item{extent}{Numeric vector \code{c(xmin, ymin, xmax, ymax)}, an
\code{sf::st_bbox()} object, or an \code{sf} geometry.
Default \code{NULL} uses the full file extent.}

\item{tile_size}{Numeric scalar.  Side length of each square tile
(in CRS units).}

\item{overlap}{Numeric scalar.  Buffer distance added around each
tile to ensure seamless coverage (default 0).}

\item{select}{Character.  Column selection string.}

\item{filter}{Character.  Attribute filter string.}

\item{max_depth}{Integer.  Maximum octree depth (\code{-1} = all).}

\item{resolution}{Numeric.  Target point spacing (in CRS units);
overrides \code{max_depth} if both are given.}

\item{max_points}{Numeric.  Per-tile max point cap.}

\item{progress}{Logical.}

\item{as_las}{Logical.  If \code{TRUE} and \pkg{lidR} is available,
each tile is returned as a \code{LAS} object instead of a raw list.}
}
\value{
A list of tile results.  Each element is the output of
\code{read_copc()} (or a \code{LAS} object if \code{as_las = TRUE}).
The list carries an attribute \code{"tile_bboxes"} with the bbox
of each tile as a 4-column matrix.
}
\description{
Divides an area of interest into a regular grid of rectangular tiles
and reads each tile independently.
}
\examples{
\donttest{
f <- system.file("extdata", "NoAZCampus_SWFSC_Bldg82_2mVoxel.copc.laz",
                 package = "copc4R")
tiles <- read_copc_tiles(f, tile_size = 200, progress = FALSE)
length(tiles)  # number of tiles
nrow(tiles[[1]]$data)  # points in first tile
}

}
