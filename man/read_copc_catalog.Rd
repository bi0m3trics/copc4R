% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{read_copc_catalog}
\alias{read_copc_catalog}
\title{Build a LAScatalog from COPC files}
\usage{
read_copc_catalog(
  paths,
  chunk_size = 0,
  chunk_buffer = 0,
  select = "*",
  filter = "",
  progress = TRUE
)
}
\arguments{
\item{paths}{Character vector.  Paths or URLs to \code{.copc.laz} files.}

\item{chunk_size}{Numeric.  Default chunk size in CRS units
(0 = file-level processing).  Sets \code{opt_chunk_size()}.}

\item{chunk_buffer}{Numeric.  Buffer around each chunk.
Sets \code{opt_chunk_buffer()}.}

\item{select}{Character.  Default column selection for processing.}

\item{filter}{Character.  Default attribute filter string.}

\item{progress}{Logical.}
}
\value{
A \code{lidR::LAScatalog} object if \pkg{lidR} is installed,
otherwise a \code{copc_catalog} S3 list with file metadata.
}
\description{
Creates a \code{lidR::LAScatalog} (when \pkg{lidR} is installed) from
one or more COPC paths/URLs.  Headers are read via the COPC hierarchy
without downloading full files, and the resulting catalog inherits
lidR processing options (\code{opt_chunk_size()}, etc.).
}
\examples{
\donttest{
f <- system.file("extdata", "NoAZCampus_SWFSC_Bldg82_2mVoxel.copc.laz",
                 package = "copc4R")
ctg <- read_copc_catalog(f, progress = FALSE)
print(ctg)
}

}
